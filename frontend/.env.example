# Base URL of the NestJS API. No trailing slash, no /api suffix needed —
# lib/api.ts appends /api itself.
# Local:      http://localhost:3000
# Production: https://laundry-shop-api.onrender.com
NEXT_PUBLIC_API_URL=http://localhost:3000
